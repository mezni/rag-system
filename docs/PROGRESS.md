# Build Progress — Aether Wireless RAG System

This log tracks the evolution of the system from single-file v0 pipelines to
a production-ready architecture. Every meaningful change — a new capability,
a refactor, a fix to a design flaw — gets a row here, in chronological order.

**How to use this file:**
- One row per step. Never edit a past row's *Problem* or *Solution* after the
  fact — if a decision was later reversed, add a new row referencing it
  (e.g. "Revisits Step 1.3") rather than rewriting history.
- `Step ID` groups related work: `0.x` = project setup, `1.x` = ingestion,
  `2.x` = retrieval, `3.x` = evaluation, `4.x` = guardrails/prompts/rollback,
  `5.x` = observability/FinOps, `6.x` = deployment/infra. Adjust as needed.
- `Status`: `done`, `in progress`, `blocked`, or `reverted`.

| Step ID | Date | Problem | Solution | What Was Added | Status |
|---------|------------|---------|----------|-----------------|--------|
| 0.1 | | No project scaffold existed. | Bootstrap a Python project with a reproducible environment. | `uv init`, `.venv` virtual environment created. | done |
| 0.2 | | Need fake Aether Wireless policy documents to work with. | Generate realistic fake documents with `generate_docs_hf` using Hugging Face. | `scripts/generate_docs_hf.py`. | done |
| 1.1 | | No way to test ingestion end to end before designing the full architecture. | Build a minimal baseline: parse one PDF, embed it, confirm the vector dimension is correct. | `ingestion.py` baseline script; tested on a sample billing dispute policy PDF; confirmed 1536-dim vectors. | done |
| 1.2 | | Need to change embedding model from paid OpenAI to free local model| Free embedding model| Add `huggingface` | done|
| 1.3 | | Single-script ingestion.py needed refactoring into modular package. | Rewrote package module split, CLI entry point | `src/ingestion/` | done |
| 1.4 | | `ingestion_state.json` creates high risk for race conditions, file corruption during unexpected crashes, and poor query scalability as document counts grow. | Replace `LocalStateStore` with PostgreSQL. Use explicit schema migrations (Alembic) and ACID-compliant transactions to guarantee state integrity for documents, chunks, and pipeline_runs. | `alembic` migration framework initialized; `src/core/models/sqlalchemy_models.py` with `DocumentOrm`, `ChunkOrm`, `PipelineRunOrm`; `src/ingestion/stages/persist.py` PostgreSQL-backed `PostgreSQLStateStore`; `src/db/` layer with schemas, repositories, and migrations; `pipeline.py` updated to use DB store; `docker/` infrastructure with `docker-compose.yml`, `Dockerfile`, `.env.example`; `alembic.ini` configured for `postgresql+psycopg2://rag_user:rag_password@localhost:15432/rag_ingestion`. | done |
| 1.5 | | Step 1.4 left dead `LocalStateStore` code and circular imports; pipeline could not run. | Finish integration: move `Settings` to `src/ingestion/config.py`, delete dead code, commit transactions in the store, wire `config/logging.yaml`. | `src/ingestion/config.py`; `Settings` fields for `DATABASE_URL`/`OPENROUTER_API_KEY`/`HF_TOKEN`/`ENVIRONMENT`; `cli.py` delegates to `pipeline.main()`; file-based logging; verified end-to-end run (102 chunks indexed) and idempotency (unchanged files skipped). | done |
| 1.6 | | A modified file overwrote its single document row — no audit trail of prior content; deleted files erased state entirely. | Add pipeline-owned versioning. Each modification creates a new `documents` row (`version` = previous + 1, `is_active` = true) and deactivates the prior versions; deletes flip `is_active` to false across all versions and chunks while preserving history. Files are keyed by canonical `source_id` (unique constraint dropped). | `version` (int) + `is_active` (bool) columns on `documents` and `chunks`; Alembic migration `add_document_versioning`; `upsert_document` returns `(doc_id, version)` and deactivates older versions (+ their chunks); `deactivate_source()` for deletions; `src/core/models/metadata.py` (`DocumentMetadata`, `ChunkMetadata`, `DataLineageMetadata` with `lineage()` builder) and `src/core/models/lineage.py`; indexes `(source_id, is_active)`, `(source_id, version)`, `(document_id, is_active)`. Verified: modify → v2 active / v1 inactive, delete → all inactive, idempotent re-run. | done |
| 1.7 | | `source_id` is the canonical *absolute* path. A container run (`/rag-system/...`) and a host run (`/home/dali/WORK/rag-system/...`) address the same file under different keys, so each re-scan deactivates the other's document versions. | Normalize `source_id` to a path relative to the scan root (with a configurable mount anchor) so host and container runs converge on the same key and stop toggling each other's versions. | `normalize_source_id()` in `pipeline.py` (scan-root-relative with `MOUNT_ANCHOR` fallback + legacy foreign-absolute detection); `Settings.mount_anchor`; diff/upsert/deactivate all keyed by the normalized id; `src/db/backfill_source_ids.py` one-time alignment (dedupes identical-content rows hard, keeps divergent rows by deactivation). Verified: host run and container run each report `unchanged=3 deleted=0`, leaving 3 canonical `billing/...` keys (102 active chunks, no cross-deactivation). | done |
| 1.8 | | PostgreSQL needed the `vector` type and filterable columns so the approved "Storage + filters" strategy could ship; host `uv` and Docker runs also diverged on DB port, entrypoint, and deps. | Storage strategy build: re-provisioned DB on `pgvector/pg15`, added `embedding_vector Vector(384)` + dedicated filter columns (`tenant_id`, `access_roles`, `category`, `department`, `classification`, `language`) via Alembic migration `add_pgvector_filters`; chunk metadata now inherits document taxonomy so each chunk is filterable without a join. Also repaired host/container parity (ports 15432→5432 everywhere, `alembic`/`sqlalchemy` pinned, `PYTHONPATH`, healthcheck gating, `alembic upgrade head` in entrypoint, `DATABASE_URL` env override). | `src/db/migrations/versions/add_pgvector_filters.py` (HNSW `ix_chunks_embedding_vector_hnsw`, GIN `ix_chunks_access_roles_gin` + btree filters); `ChunkOrm.embedding_vector` + filter columns; `persist.py` `_filter_fields()`, `upsert_chunk` vector/filter writes, `search_chunks(embedding, tenant_id, access_roles, category, is_active, limit)` = RBAC pre-filter + cosine distance (`[0,2]`); `metadata.py` `build_chunk_metadata` taxonomy inheritance; `docker-compose.yml`/`.env`/`alembic.ini`/`Dockerfile`/`docker/entrypoint.sh`/`env.py` parity fixes. Verified: 102-chunk clean re-ingest writes vectors+filters via pipeline; filtered query (tenant/roles/category) returns rows, non-matching category returns 0; unfiltered ANN query plan uses HNSW index. | done |
| 1.9 | | Extractors only handled PDF/Markdown/plain text via ad-hoc parsers (`pypdf`, regex, raw text) — no path for the unstructured formats Aether actually stores policies in (Word, Excel, HTML, slides, JSON/XML exports, rate-card CSVs). | Replace hand-rolled parsers with the LlamaIndex extraction framework. `parse.py` now routes every supported extension through a registered LlamaIndex `Reader` (`PDFReader`, `MarkdownReader`, `FlatReader`, `DocxReader`, `HTMLTagReader`, `CSVReader`, `PandasExcelReader`, `PptxReader`, `JSONReader`, `XMLReader`, `RTFReader`, `EpubReader`, `IPYNBReader`), joins node texts into one `ParsedContent`, carries block-level `page`/`char_start` lineage from reader metadata, merges scalar metadata, and falls back to raw-text capture if a reader returns no text. Failure wrapper keeps the pipeline's per-file skip-and-continue semantics. | `src/ingestion/stages/parse.py` (LlamaIndex `LlamaIndexExtractor`, `EXTRACTORS` registry, `PARSER_ENGINE` label map); `config.py` `supported_extensions` widened to 17 formats; `pipeline.py` re-pointed at shared `PARSER_ENGINE` (engine label now `llamaindex.<Reader>` in chunk/doc metadata); LLamaIndex deps (`llama-index-core`, `llama-index-readers-file`, `llama-index-readers-json` + `python-docx`, `docx2txt`, `python-pptx`, `openpyxl`, `pandas`, `ebooklib`, `beautifulsoup4`) added to `pyproject.toml`. Verified: 8 mixed-format files (docx/html/csv/xlsx/pptx/json/xml/txt) ingested end-to-end (8 chunks), PDF keeps page lineage (10 pages), Markdown keeps section lineage (44 blocks). | done |
| 1.10 | | Fixed character-length chunking (`CHUNK_SIZE_CHARS`) severs contextual continuity across logical section boundaries and table rows; each record carried no understanding of document hierarchy. | Shift to structural, metadata-aware semantic chunking on LlamaIndex. The extractor now emits structural `blocks` (Markdown sections with `header_path` breadcrumbs via `MarkdownNodeParser`, PDF pages, table rows/sheets, doc nodes) with `char_start`/`char_end`, `page`, and `kind`. The chunker groups consecutive blocks up to the char budget and only ever cuts at block boundaries (a section or table row is never sliced mid-content); oversized blocks are recursively split with LlamaIndex `SentenceSplitter` (token-budgeted, sentence-aware, cl100k tokenizer), each piece inheriting the parent's hierarchy tags. Every stored record carries `header_path`, `sections` (breadcrumbs covered), `chunk_kind` (section/table/text), and page spans, and the pipeline stamps these into `ChunkMetadata` so retrieval can filter/audit by section. Legacy char splitter kept behind `CHUNKING_STRATEGY=fixed` (default `semantic`). | `parse.py`: `ParsedContent.blocks`, `LlamaIndexExtractor` block builders (`_doc_blocks`, `_markdown_blocks`), `BLOCK_KINDS`, `EXTRACTORS` `concat_rows=False` for CSV/XLSX (one block per table row); `chunk.py`: `split_block` (SentenceSplitter + char fallback), `_record_from_group`, `build_chunk_records(..., strategy)`; `config.py` `chunking_strategy`; `metadata.py` `ChunkMetadata.sections` + `chunk_kind`; `pipeline.py` threads `header_path`/`sections`/`chunk_kind` into `build_chunk_metadata`. Verified: bill markdown yields 26 section-aware chunks (vs 31 fixed), breadcrumbs like `Billing Cycle Rules > Invoicing and Due Date` persisted in `chunks.lineage`; oversized `Scope and Applicability` section recursively split into pieces retaining `header_path`; PDF chunks never cross page boundaries (`page_start`/`page_end`); CSV rows grouped as atomic `kind=table` chunks. | done |
| 1.11 | | No automated test suite: extraction, chunking, versioning, and search relied on manual end-to-end runs only. | Add pytest unit + integration coverage. Unit tests (no DB, no torch) cover cleaning, legacy + semantic chunking (section-boundary cuts, oversized-block recursive split with fallback, atomic table rows, fixed strategy), every LlamaIndex extractor registry entry, markdown `header_path` breadcrumbs, plain-text/CSV/HTML parsing, `FileProcessingError` on corrupt PDFs, metadata lineage, config env overrides, source-id normalization, file diff classification, and store filter-field extraction. Integration tests run the real pipeline against PostgreSQL (pgvector) via the existing Docker `db` service; `embed_chunks` is stubbed so torch/sentence-transformers never load, while versioning, idempotency, deactivation, `search_chunks`, and `lineage.header_path`/`chunk_kind` persistence are asserted end-to-end. Integration skips cleanly when the DB is unreachable. | `pyproject.toml` `[dependency-groups] dev` (pytest, pytest-cov, ruff) + `[tool.pytest.ini_options]` (markers, testpaths); `tests/conftest.py` DB-reachability gate; `tests/unit/` `test_clean.py`, `test_chunk.py`, `test_parse.py`, `test_metadata.py`, `test_pipeline.py`, `test_config.py`, `test_persist.py`; `tests/integration/test_pipeline_ingest.py` (ingest+search, idempotent rerun, modify→v2/deactivated v1, delete→deactivated) with per-test DB isolation/cleanup; `src/ingestion/stages/parse.py` reader imports pointed at lightweight `llama_index.readers.file.*.base` modules; `src/ingestion/stages/embed.py` torch import made lazy. `make test` (`uv run pytest`) now green. Verified: 45 passed twice consecutively (hermetic); `ruff check --fix` clean on changed files. | done |
| 5.1 | | Ops visibility was developer-only: run stats and chunk lineage lived in Postgres with no human-facing view, and the README called for a "Streamlit ops dashboard" that didn't exist. | Add a Streamlit multipage ops dashboard (`src/ui/`). `app.py` is the entrypoint (connectivity check); `pages/runs_dashboard.py` shows pipeline run KPIs (totals by status, chunks/files written across runs), a sortable recent-runs table with duration/status/stats, and a per-run inspector with raw stats JSON and error text; `pages/chunk_explorer.py` shows corpus KPIs (active chunks/documents, % embedded), sidebar filters (category, `chunk_kind` from `lineage`, has-embedding, content search, document, limit), an exportable chunk table with breadcrumb/pages/char-span lineage, per-chunk expanders with full content, and a per-source document summary with active-chunk counts. Queries run against the same ORM models the pipeline writes, through a cached Streamlit session factory (pool_pre_ping; dead DB degrades to an error banner, not a crash). | `streamlit>=1.36` in `pyproject.toml`; `Makefile` `ui` target (`PYTHONPATH=. uv run streamlit run src/ui/app.py`); `src/ui/db.py` (sys.path bootstrap + `@st.cache_resource` session factory + `session_scope`); `src/ui/app.py`; `src/ui/pages/runs_dashboard.py`; `src/ui/pages/chunk_explorer.py`; `src/ui/__init__.py`/`pages/__init__.py`. Verified: `make ui` boots at `http://localhost:8501` with auto-discovered pages; all three scripts and widget reruns (status filter, content search, `kind=table` filter) execute clean against the live pgvector DB via Streamlit `AppTest`; deprecated `use_container_width` replaced with `width="stretch"`; ruff clean. | done |